/** Sem ping por este intervalo → sessão encerrada (5 min). */
export const PRESENCE_IDLE_MS = 5 * 60 * 1000

/** Intervalo do heartbeat no cliente (60 s). */
export const PRESENCE_HEARTBEAT_MS = 60 * 1000
